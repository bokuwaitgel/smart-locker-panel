# Backend API base URL (no trailing slash, no /api prefix — routes are /auth/login, /lockers, ...)
# prod:  https://247box.ekzomap.mn
# local: http://localhost:3030
NEXT_PUBLIC_API_URL=https://247box.ekzomap.mn
